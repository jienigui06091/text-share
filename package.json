{
  "name": "text-relay",
  "version": "1.0.0",
  "private": true,
  "description": "A temporary, private text relay built for Cloudflare Workers.",
  "scripts": {
    "dev": "wrangler dev",
    "dev:lan": "wrangler dev --ip 0.0.0.0 --port 8787",
    "check": "node --check src/index.js && node --check src/html.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "s3mini": "^1.0.0"
  }
}
